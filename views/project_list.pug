extends layout

block content
  h1= title
  
  ul    
    each project in book_list
      li 
        a(href="project.name") #{project.name} 
        |  (#{project.origin})

    else
      li Nenhum projeto cadastrado.